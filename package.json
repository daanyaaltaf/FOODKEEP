{
  "name": "foodkeep",
  "version": "1.0.0",
  "description": "Foodkeep — personal food & waste tracker. Express backend + SQLite (node:sqlite) serving the single-page frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18"
  }
}
